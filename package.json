{
  "name": "cms-audio-lambda",
  "version": "0.0.0",
  "description": "Lambda audio processing for cms.prx.org audio file uploads",
  "main": "index.js",
  "scripts": {
    "start": "npm test",
    "test": "mocha test/**",
    "ci": "istanbul cover _mocha -- test/** && codecov",
    "watch": "mocha test/** -w",
    "deploy-dev": "AWS_ENVIRONMENT=development node-lambda deploy --configFile config/development.env",
    "deploy-staging": "AWS_ENVIRONMENT=staging node-lambda deploy --configFile config/staging.env",
    "deploy-prod": "AWS_ENVIRONMENT=production node-lambda deploy --configFile config/production.env"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/PRX/cms-audio-lambda"
  },
  "author": "ryan cavis <ryan@prx.org>",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.4.7",
    "fluent-ffmpeg": "^2.1.0",
    "q": "^1.4.1",
    "request": "^2.73.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "codecov": "^1.0.1",
    "dotenv": "^2.0.0",
    "istanbul": "^0.4.4",
    "mocha": "^2.5.3",
    "node-lambda": "^0.8.6",
    "sinon": "^1.17.5"
  }
}
